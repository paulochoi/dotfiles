require("noice").setup({
  notify = {
    enabled = false,
    view = "notify",
  },
  messages = {
    enabled = false,
  },
  popupmenu = {
    enabled = false
  },
  hover = {
    enabled = false
  }
})
